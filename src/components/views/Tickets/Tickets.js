import React from 'react';
import MainSection from '../../layout/MainSection/MainSection';

const Tickets = () => {
  return (
    <section>
      <MainSection variant={'tickets'} />
    </section>
  );
};

export default Tickets;
